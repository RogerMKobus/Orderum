import React from 'react';
import {
  ScrollView,
} from 'react-native';

import Header from './src/components/Header'

const App = () => {
  return (
    <ScrollView>
      <Header />
    </ScrollView>
  );
};

export default App;
